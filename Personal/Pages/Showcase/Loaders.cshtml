@page
@model Personal.Pages.Showcase.LoadersModel
@{
    ViewData["Title"] = "Loaders";
}

<div class="showcase-page">
    <div class="showcase-header">
        <h1>Loaders &amp; Spinners</h1>
        <p>Smooth loading indicators from orbital spinners to 3D cubes, radar sweeps, and skeleton placeholders.</p>
    </div>

    <div class="showcase-grid">

        <!-- Orbit Spinner -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Orbit Spinner <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <div class="loader-orbit">
                    <div class="orbit-ring"></div>
                    <div class="orbit-ring"></div>
                    <div class="orbit-ring"></div>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">HTML + CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-markup">&lt;div class="loader-orbit"&gt;
  &lt;div class="orbit-ring"&gt;&lt;/div&gt;
  &lt;div class="orbit-ring"&gt;&lt;/div&gt;
  &lt;div class="orbit-ring"&gt;&lt;/div&gt;
&lt;/div&gt;</code></pre>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.loader-orbit {
  position: relative;
  width: 60px; height: 60px;
}

.loader-orbit .orbit-ring {
  position: absolute;
  inset: 0;
  border: 2px solid transparent;
  border-radius: 50%;
}

.orbit-ring:nth-child(1) {
  border-top-color: #00e5ff;
  animation: orbit 1.2s linear infinite;
}

.orbit-ring:nth-child(2) {
  inset: 6px;
  border-right-color: #8b5cf6;
  animation: orbit 1.6s linear infinite reverse;
}

.orbit-ring:nth-child(3) {
  inset: 12px;
  border-bottom-color: #ec4899;
  animation: orbit 2s linear infinite;
}

@@keyframes orbit {
  to { transform: rotate(360deg); }
}</code></pre>
            </div>
        </div>

        <!-- Morphing Dots -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Morphing Dots <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <div class="loader-dots">
                    <div class="dot"></div>
                    <div class="dot"></div>
                    <div class="dot"></div>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.loader-dots {
  display: flex;
  gap: 8px;
  align-items: center;
}

.loader-dots .dot {
  width: 14px; height: 14px;
  border-radius: 50%;
  animation: morphDot 1.4s ease-in-out infinite;
}

.dot:nth-child(1) {
  background: #00e5ff;
  animation-delay: 0s;
}
.dot:nth-child(2) {
  background: #8b5cf6;
  animation-delay: 0.2s;
}
.dot:nth-child(3) {
  background: #ec4899;
  animation-delay: 0.4s;
}

@@keyframes morphDot {
  0%, 100% {
    transform: scale(1) translateY(0);
    border-radius: 50%;
  }
  25% {
    transform: scale(1.3) translateY(-10px);
    border-radius: 30%;
  }
  50% {
    transform: scale(1) translateY(0);
    border-radius: 50%;
  }
}</code></pre>
            </div>
        </div>

        <!-- Skeleton Loading -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Skeleton Loading <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <div class="loader-skeleton">
                    <div class="skeleton-circle"></div>
                    <div class="skeleton-line"></div>
                    <div class="skeleton-line medium"></div>
                    <div class="skeleton-line short"></div>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.loader-skeleton {
  width: 280px;
  padding: 1.5rem;
  background: rgba(255,255,255,0.03);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 12px;
}

.skeleton-line {
  height: 12px;
  background: linear-gradient(
    90deg,
    rgba(255,255,255,0.04) 25%,
    rgba(255,255,255,0.08) 50%,
    rgba(255,255,255,0.04) 75%
  );
  background-size: 200% 100%;
  border-radius: 6px;
  animation: shimmer 1.5s ease-in-out infinite;
  margin-bottom: 0.75rem;
}

.skeleton-line.short { width: 60%; }
.skeleton-line.medium { width: 80%; }

.skeleton-circle {
  width: 48px; height: 48px;
  border-radius: 50%;
  background: linear-gradient(
    90deg,
    rgba(255,255,255,0.04) 25%,
    rgba(255,255,255,0.08) 50%,
    rgba(255,255,255,0.04) 75%
  );
  background-size: 200% 100%;
  animation: shimmer 1.5s ease-in-out infinite;
  margin-bottom: 1rem;
}

@@keyframes shimmer {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}</code></pre>
            </div>
        </div>

        <!-- Morph Loader -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Morph Loader <span class="badge">CSS + JS</span></h3>
            <div class="preview-panel">
                <div class="loader-morph-container">
                    <div class="loader-morph">
                        <div class="morph-shape-demo"></div>
                    </div>
                    <span class="morph-loader-text">Loading</span>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">HTML + CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.loader-morph-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2rem;
}

.loader-morph {
  width: 80px;
  height: 80px;
}

.morph-shape-demo {
  width: 100%;
  height: 100%;
  background: linear-gradient(135deg, #00e5ff, #8b5cf6, #ec4899);
  background-size: 400% 400%;
  animation:
    morphShape 3s ease-in-out infinite,
    morphGradient 4s ease infinite,
    morphSpin 6s linear infinite;
  box-shadow:
    0 0 40px rgba(0,229,255,0.3),
    0 0 80px rgba(139,92,246,0.15);
}

@@keyframes morphShape {
  0%, 100% { border-radius: 50%; }
  25% { border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%; }
  50% { border-radius: 50% 20% 50% 20% / 20% 50% 20% 50%; }
  75% { border-radius: 70% 30% 30% 70% / 70% 70% 30% 30%; }
}

@@keyframes morphGradient {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

@@keyframes morphSpin {
  to { transform: rotate(360deg); }
}

.morph-loader-text {
  color: #71717a;
  font-size: 0.85rem;
  font-weight: 600;
  letter-spacing: 0.3em;
  text-transform: uppercase;
  animation: morphPulse 2s ease-in-out infinite;
}

@@keyframes morphPulse {
  0%, 100% { opacity: 0.4; }
  50% { opacity: 1; }
}</code></pre>
            </div>
        </div>

        <!-- Helix Spinner -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Helix Spinner <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <div class="loader-helix"></div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.loader-helix {
  width: 50px; height: 50px;
  position: relative;
}

.loader-helix::before,
.loader-helix::after {
  content: '';
  position: absolute; inset: 0;
  border: 3px solid transparent;
  border-radius: 50%;
}

.loader-helix::before {
  border-top-color: #00e5ff;
  border-bottom-color: #00e5ff;
  animation: helixSpin 1.5s linear infinite;
}

.loader-helix::after {
  border-left-color: #8b5cf6;
  border-right-color: #8b5cf6;
  animation: helixSpin 1.5s linear infinite reverse;
}

@@keyframes helixSpin {
  to { transform: rotate(360deg); }
}</code></pre>
            </div>
        </div>

        <!-- Neon Line -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Neon Line <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <div class="loader-neon-line"></div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.loader-neon-line {
  width: 200px; height: 3px;
  background: rgba(255,255,255,0.06);
  border-radius: 3px;
  overflow: hidden; position: relative;
}

.loader-neon-line::before {
  content: '';
  position: absolute;
  top: 0; left: -40%;
  width: 40%; height: 100%;
  background: linear-gradient(90deg,
    transparent, #00e5ff, transparent);
  border-radius: 3px;
  animation: neonSlide 1.4s ease-in-out infinite;
  box-shadow: 0 0 12px #00e5ff,
    0 0 30px rgba(0,229,255,0.25);
}

@@keyframes neonSlide {
  0% { left: -40%; }
  100% { left: 100%; }
}</code></pre>
            </div>
        </div>

        <!-- Wave Bars -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Wave Bars <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <div class="loader-wave">
                    <div class="bar"></div>
                    <div class="bar"></div>
                    <div class="bar"></div>
                    <div class="bar"></div>
                    <div class="bar"></div>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.loader-wave {
  display: flex; gap: 4px;
  align-items: flex-end; height: 40px;
}

.loader-wave .bar {
  width: 5px; border-radius: 3px;
  animation: waveBar 1.2s ease-in-out infinite;
}

.bar:nth-child(1) { background:#00e5ff; }
.bar:nth-child(2) { background:#06b6d4; animation-delay:.1s; }
.bar:nth-child(3) { background:#8b5cf6; animation-delay:.2s; }
.bar:nth-child(4) { background:#a855f7; animation-delay:.3s; }
.bar:nth-child(5) { background:#ec4899; animation-delay:.4s; }

@@keyframes waveBar {
  0%, 100% { height: 10px; opacity: 0.4; }
  50% { height: 40px; opacity: 1; }
}</code></pre>
            </div>
        </div>

        <!-- Grid Pulse -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Grid Pulse <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <div class="loader-grid">
                    <div class="cell"></div>
                    <div class="cell"></div>
                    <div class="cell"></div>
                    <div class="cell"></div>
                    <div class="cell"></div>
                    <div class="cell"></div>
                    <div class="cell"></div>
                    <div class="cell"></div>
                    <div class="cell"></div>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.loader-grid {
  display: grid;
  grid-template-columns: repeat(3, 14px);
  gap: 6px;
}

.loader-grid .cell {
  width: 14px; height: 14px;
  border-radius: 3px;
  background: #00e5ff;
  animation: gridPulse 1.4s ease-in-out infinite;
}

/* Stagger each cell 0.1s apart */
.cell:nth-child(1) { animation-delay: 0s; }
.cell:nth-child(2) { animation-delay: .1s; }
.cell:nth-child(3) { animation-delay: .2s; }
/* ...through nth-child(9) at 0.8s */

@@keyframes gridPulse {
  0%, 100% { opacity: .15; transform: scale(.8); }
  50% { opacity: 1; transform: scale(1); }
}</code></pre>
            </div>
        </div>

        <!-- 3D Cube -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">3D Cube <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <div class="loader-cube-scene">
                    <div class="loader-cube">
                        <div class="face"></div>
                        <div class="face"></div>
                        <div class="face"></div>
                        <div class="face"></div>
                        <div class="face"></div>
                        <div class="face"></div>
                    </div>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.loader-cube-scene {
  width: 40px; height: 40px;
  perspective: 200px;
}

.loader-cube {
  width: 100%; height: 100%;
  position: relative;
  transform-style: preserve-3d;
  animation: cubeFlip 2.4s ease-in-out infinite;
}

.loader-cube .face {
  position: absolute; inset: 0;
  border: 2px solid #00e5ff;
  background: rgba(0,229,255,0.04);
  border-radius: 2px;
}

.face:nth-child(1) { transform: translateZ(20px); }
.face:nth-child(2) { transform: rotateY(180deg) translateZ(20px); }
.face:nth-child(3) { transform: rotateY(90deg) translateZ(20px); }
.face:nth-child(4) { transform: rotateY(-90deg) translateZ(20px); }
.face:nth-child(5) { transform: rotateX(90deg) translateZ(20px); }
.face:nth-child(6) { transform: rotateX(-90deg) translateZ(20px); }

@@keyframes cubeFlip {
  0%   { transform: rotateX(0) rotateY(0); }
  25%  { transform: rotateX(90deg) rotateY(0); }
  50%  { transform: rotateX(90deg) rotateY(90deg); }
  75%  { transform: rotateX(180deg) rotateY(90deg); }
  100% { transform: rotateX(180deg) rotateY(180deg); }
}</code></pre>
            </div>
        </div>

        <!-- Radar Sweep -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Radar Sweep <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <div class="loader-radar">
                    <div class="radar-ring"></div>
                    <div class="radar-ring"></div>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.loader-radar {
  width: 60px; height: 60px;
  border-radius: 50%;
  position: relative;
  border: 1.5px solid rgba(0,229,255,0.15);
}

/* Sweep beam */
.loader-radar::before {
  content: '';
  position: absolute; inset: 0;
  border-radius: 50%;
  background: conic-gradient(
    from 0deg,
    transparent 0%, transparent 75%,
    rgba(0,229,255,0.35) 100%
  );
  animation: radarSweep 1.8s linear infinite;
}

/* Center dot */
.loader-radar::after {
  content: '';
  position: absolute;
  top: 50%; left: 50%;
  width: 6px; height: 6px;
  background: #00e5ff;
  border-radius: 50%;
  transform: translate(-50%, -50%);
  box-shadow: 0 0 8px rgba(0,229,255,0.5);
}

@@keyframes radarSweep {
  to { transform: rotate(360deg); }
}</code></pre>
            </div>
        </div>

        <!-- Plasma Ring -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Plasma Ring <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <div class="loader-plasma"></div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.loader-plasma {
  width: 60px; height: 60px;
  border-radius: 50%;
  position: relative;
}

.loader-plasma::before,
.loader-plasma::after {
  content: '';
  position: absolute;
  border-radius: 50%;
  background: conic-gradient(
    #00e5ff, #8b5cf6, #ec4899,
    #f59e0b, #10b981, #00e5ff
  );
  animation: plasmaRotate 1.5s linear infinite;
}

/* Sharp ring */
.loader-plasma::before {
  inset: 0;
  padding: 3px;
  -webkit-mask:
    linear-gradient(#fff 0 0) content-box,
    linear-gradient(#fff 0 0);
  -webkit-mask-composite: xor;
  mask-composite: exclude;
}

/* Glow ring */
.loader-plasma::after {
  inset: -6px;
  padding: 5px;
  filter: blur(10px);
  opacity: 0.5;
  -webkit-mask:
    linear-gradient(#fff 0 0) content-box,
    linear-gradient(#fff 0 0);
  -webkit-mask-composite: xor;
  mask-composite: exclude;
}

@@keyframes plasmaRotate {
  to { transform: rotate(360deg); }
}</code></pre>
            </div>
        </div>

        <!-- Pendulum Wave -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Pendulum Wave <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <div class="loader-pendulum">
                    <div class="pend"></div>
                    <div class="pend"></div>
                    <div class="pend"></div>
                    <div class="pend"></div>
                    <div class="pend"></div>
                    <div class="pend"></div>
                    <div class="pend"></div>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.loader-pendulum {
  display: flex; gap: 8px;
  align-items: flex-start; height: 70px;
}

.loader-pendulum .pend {
  width: 2px; height: 44px;
  background: rgba(255,255,255,0.08);
  transform-origin: top center;
  animation: pendSwing 1.6s ease-in-out infinite;
}

.loader-pendulum .pend::after {
  content: '';
  position: absolute;
  bottom: -5px; left: 50%;
  width: 10px; height: 10px;
  border-radius: 50%;
  transform: translateX(-50%);
  background: #00e5ff;
  box-shadow: 0 0 8px rgba(0,229,255,0.4);
}

/* Each pendulum uses a slightly different
   duration to create the wave effect */
.pend:nth-child(2) { animation-duration: 1.7s; }
.pend:nth-child(3) { animation-duration: 1.8s; }
.pend:nth-child(4) { animation-duration: 1.9s; }
.pend:nth-child(5) { animation-duration: 2.0s; }
.pend:nth-child(6) { animation-duration: 2.1s; }
.pend:nth-child(7) { animation-duration: 2.2s; }

@@keyframes pendSwing {
  0%, 100% { transform: rotate(25deg); }
  50% { transform: rotate(-25deg); }
}</code></pre>
            </div>
        </div>

        <!-- Newton's Cradle -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Newton's Cradle <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <div class="loader-newton">
                    <div class="newton-ball"></div>
                    <div class="newton-ball"></div>
                    <div class="newton-ball"></div>
                    <div class="newton-ball"></div>
                    <div class="newton-ball"></div>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.loader-newton {
  display: flex;
  align-items: flex-start;
  height: 64px; gap: 0;
}

.newton-ball {
  width: 14px; height: 42px;
  position: relative;
  transform-origin: top center;
}

/* String */
.newton-ball::before {
  content: '';
  position: absolute; top: 0; left: 50%;
  width: 1px; height: 28px;
  background: rgba(255,255,255,0.12);
  transform: translateX(-50%);
}

/* Ball */
.newton-ball::after {
  content: '';
  position: absolute; bottom: 0; left: 50%;
  width: 14px; height: 14px;
  border-radius: 50%;
  transform: translateX(-50%);
  background: linear-gradient(
    135deg, rgba(255,255,255,0.2),
    rgba(255,255,255,0.05)
  );
  border: 1px solid rgba(255,255,255,0.08);
}

.newton-ball:first-child {
  animation: newtonL 1.2s ease-in-out infinite;
}
.newton-ball:first-child::after {
  background: linear-gradient(135deg, #00e5ff, #0891b2);
  border: none;
  box-shadow: 0 0 12px rgba(0,229,255,0.3);
}

.newton-ball:last-child {
  animation: newtonR 1.2s ease-in-out infinite;
}
.newton-ball:last-child::after {
  background: linear-gradient(135deg, #8b5cf6, #7c3aed);
  border: none;
  box-shadow: 0 0 12px rgba(139,92,246,0.3);
}

@@keyframes newtonL {
  0%      { transform: rotate(0); }
  25%     { transform: rotate(-30deg); }
  50%,100%{ transform: rotate(0); }
}
@@keyframes newtonR {
  0%,50%  { transform: rotate(0); }
  75%     { transform: rotate(30deg); }
  100%    { transform: rotate(0); }
}</code></pre>
            </div>
        </div>

        <!-- Glitch Bar -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Glitch Bar <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <div class="loader-glitch-bar">
                    <div class="glitch-track"></div>
                    <div class="glitch-ghost"></div>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.loader-glitch-bar {
  width: 180px; height: 4px;
  background: rgba(255,255,255,0.06);
  border-radius: 4px;
  position: relative;
  overflow: hidden;
}

/* Main beam */
.glitch-track {
  position: absolute; top: 0;
  width: 40%; height: 100%;
  background: #00e5ff;
  border-radius: 4px;
  animation: glitchSlide 1.8s ease-in-out infinite;
  box-shadow: 0 0 12px rgba(0,229,255,0.5);
}

/* Chromatic ghost (pink offset) */
.glitch-ghost {
  position: absolute; top: -2px;
  width: 40%; height: calc(100% + 4px);
  background: #ec4899;
  border-radius: 4px;
  animation: glitchSlide 1.8s ease-in-out infinite,
    glitchJitter 1.8s step-end infinite;
  opacity: 0.35;
  filter: blur(2px);
}

@@keyframes glitchSlide {
  0%   { left: -40%; }
  100% { left: 100%; }
}
@@keyframes glitchJitter {
  0%  { transform: translateY(0); }
  15% { transform: translateY(-3px); }
  17% { transform: translateY(2px); }
  19% { transform: translateY(0); }
  50% { transform: translateY(0); }
  65% { transform: translateY(3px); }
  67% { transform: translateY(-2px); }
  69% { transform: translateY(0); }
}</code></pre>
            </div>
        </div>

        <!-- Heartbeat -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Heartbeat <span class="badge">SVG + CSS</span></h3>
            <div class="preview-panel">
                <div class="loader-heartbeat-wrap">
                    <svg class="loader-heartbeat" viewBox="0 0 200 60" width="200" height="60">
                        <polyline class="hb-line" points="0,30 50,30 60,30 68,8 76,52 84,22 90,38 96,30 200,30" />
                    </svg>
                    <div class="hb-glow"></div>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">SVG + CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-markup">&lt;svg class="loader-heartbeat" viewBox="0 0 200 60"&gt;
  &lt;polyline class="hb-line"
    points="0,30 50,30 60,30 68,8
            76,52 84,22 90,38 96,30 200,30"/&gt;
&lt;/svg&gt;

&lt;style&gt;
.hb-line {
  fill: none;
  stroke: #00e5ff;
  stroke-width: 2;
  stroke-linecap: round;
  stroke-linejoin: round;
  stroke-dasharray: 20 280;
  stroke-dashoffset: 0;
  animation: heartDraw 1.8s linear infinite;
  filter:
    drop-shadow(0 0 3px rgba(0,229,255,0.6));
}

@@keyframes heartDraw {
  to { stroke-dashoffset: -300; }
}
&lt;/style&gt;</code></pre>
            </div>
        </div>

        <!-- Breathing Orb -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Breathing Orb <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <div class="loader-breathe">
                    <div class="breathe-core"></div>
                    <div class="breathe-ring"></div>
                    <div class="breathe-ring breathe-ring--2"></div>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.loader-breathe {
  width: 70px; height: 70px;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.breathe-core {
  width: 20px; height: 20px;
  border-radius: 50%;
  background: radial-gradient(
    circle, #00e5ff, #0891b2
  );
  box-shadow: 0 0 20px rgba(0,229,255,0.5),
    0 0 60px rgba(0,229,255,0.15);
  animation: breatheIn 3s ease-in-out infinite;
  z-index: 1;
}

.breathe-ring {
  position: absolute; inset: 0;
  border: 1px solid rgba(0,229,255,0.15);
  border-radius: 50%;
  animation: breatheRing 3s ease-in-out infinite;
}

.breathe-ring--2 {
  inset: -10px;
  border-color: rgba(139,92,246,0.1);
  animation-delay: 0.4s;
}

@@keyframes breatheIn {
  0%, 100% { transform: scale(0.85); opacity: 0.7; }
  50% { transform: scale(1.15); opacity: 1; }
}
@@keyframes breatheRing {
  0%, 100% { transform: scale(0.9); opacity: 0.3; }
  50% { transform: scale(1.1); opacity: 0.8; }
}</code></pre>
            </div>
        </div>

    </div>
</div>