@page
@model Personal.Pages.Showcase.TextEffectsModel
@{
    ViewData["Title"] = "Text Effects";
}

<div class="showcase-page">
    <div class="showcase-header">
        <h1>Text Effects</h1>
        <p>Eye-catching typographic effects from animated gradients to glitch distortions and liquid water fills.</p>
    </div>

    <div class="showcase-grid">

        <!-- Animated Gradient Text -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Animated Gradient <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <span class="text-gradient-animated">Flowing Colors</span>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.text-gradient-animated {
  font-size: 3.5rem;
  font-weight: 900;
  background: linear-gradient(
    90deg,
    #00e5ff, #8b5cf6, #ec4899, #00e5ff
  );
  background-size: 200% auto;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  animation: textGradient 3s linear infinite;
}

@@keyframes textGradient {
  to { background-position: 200% center; }
}</code></pre>
            </div>
        </div>

        <!-- Glitch Text -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Glitch Distortion <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <span class="text-glitch" data-text="GLITCH">GLITCH</span>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">HTML + CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-markup">&lt;!-- HTML: data-text must match inner text --&gt;
&lt;span class="text-glitch" data-text="GLITCH"&gt;
  GLITCH
&lt;/span&gt;</code></pre>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.text-glitch {
  position: relative;
  font-size: 3.5rem;
  font-weight: 900;
  letter-spacing: 0.05em;
  color: #fff;
  text-transform: uppercase;
}

.text-glitch::before,
.text-glitch::after {
  content: attr(data-text);
  position: absolute;
  top: 0; left: 0;
  width: 100%; height: 100%;
}

.text-glitch::before {
  color: #00e5ff;
  animation: glitch1 2s infinite linear
    alternate-reverse;
  clip-path: polygon(0 0, 100% 0, 100% 35%, 0 35%);
}

.text-glitch::after {
  color: #ec4899;
  animation: glitch2 2s infinite linear
    alternate-reverse;
  clip-path: polygon(0 65%, 100% 65%, 100% 100%, 0 100%);
}

@@keyframes glitch1 {
  0%, 90%, 100% { transform: translate(0); }
  92% { transform: translate(-4px, 2px); }
  94% { transform: translate(3px, -1px); }
  96% { transform: translate(-2px, -3px); }
  98% { transform: translate(4px, 1px); }
}

@@keyframes glitch2 {
  0%, 90%, 100% { transform: translate(0); }
  91% { transform: translate(3px, -2px); }
  93% { transform: translate(-4px, 1px); }
  95% { transform: translate(2px, 3px); }
  97% { transform: translate(-3px, -1px); }
}</code></pre>
            </div>
        </div>

        <!-- Typewriter Text -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Typewriter <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <div class="text-typewriter-wrapper">
                    <span class="text-typewriter">console.log("Hello!");</span>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.text-typewriter {
  font-family: 'JetBrains Mono', monospace;
  font-size: 1.8rem;
  font-weight: 600;
  color: #10b981;
  overflow: hidden;
  white-space: nowrap;
  border-right: 2px solid #10b981;
  width: 0;
  animation:
    typing 3s steps(22) 0.5s forwards,
    blink 0.8s step-end infinite;
}

/* Adjust steps() to match character count */

@@keyframes typing {
  to { width: 100%; }
}

@@keyframes blink {
  50% { border-color: transparent; }
}</code></pre>
            </div>
        </div>

        <!-- Neon Sign Text -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Neon Sign <span class="badge">CSS</span></h3>
            <div class="preview-panel" style="background: rgba(0,0,0,0.3);">
                <span class="text-neon">NEON</span>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.text-neon {
  font-size: 3.5rem;
  font-weight: 900;
  color: #fff;
  text-shadow:
    0 0 7px rgba(0,229,255,0.7),
    0 0 10px rgba(0,229,255,0.5),
    0 0 21px rgba(0,229,255,0.4),
    0 0 42px rgba(0,229,255,0.3),
    0 0 82px rgba(0,229,255,0.2),
    0 0 92px rgba(0,229,255,0.1);
  animation: neonFlicker 3s infinite;
}

@@keyframes neonFlicker {
  0%, 19%, 21%, 23%, 25%,
  54%, 56%, 100% {
    text-shadow:
      0 0 7px rgba(0,229,255,0.7),
      0 0 10px rgba(0,229,255,0.5),
      0 0 21px rgba(0,229,255,0.4),
      0 0 42px rgba(0,229,255,0.3),
      0 0 82px rgba(0,229,255,0.2),
      0 0 92px rgba(0,229,255,0.1);
  }
  20%, 24%, 55% {
    text-shadow: none;
  }
}</code></pre>
            </div>
        </div>

        <!-- Liquid Fill Text -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Liquid Fill <span class="badge badge-water">CSS</span></h3>
            <div class="preview-panel">
                <span class="text-liquid-fill">AQUA</span>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.text-liquid-fill {
  font-size: 3.5rem;
  font-weight: 900;
  text-transform: uppercase;
  background: linear-gradient(
    0deg,
    #00e5ff 0%,
    #00b4d8 35%,
    rgba(0,229,255,0.25) 48%,
    rgba(255,255,255,0.08) 56%,
    rgba(255,255,255,0.08) 100%
  );
  background-size: 100% 300%;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  animation: liquidFill 3s ease-in-out
    infinite alternate;
}

@@keyframes liquidFill {
  0% { background-position: 0% 100%; }
  100% { background-position: 0% 0%; }
}</code></pre>
            </div>
        </div>

        <!-- Water Reflection Text -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Water Reflection <span class="badge badge-water">CSS</span></h3>
            <div class="preview-panel">
                <div class="text-water-reflection-wrap">
                    <span class="text-water-main">OCEAN</span>
                    <span class="text-water-reflect" aria-hidden="true">OCEAN</span>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">HTML + CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.text-water-main {
  font-size: 3.5rem;
  font-weight: 900;
  letter-spacing: 0.08em;
  color: #fff;
  text-transform: uppercase;
  text-shadow: 0 0 20px rgba(0,229,255,0.3);
}

.text-water-reflect {
  font-size: 3.5rem;
  font-weight: 900;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  background: linear-gradient(
    180deg,
    rgba(0,229,255,0.4),
    transparent 80%
  );
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  transform: scaleY(-1);
  filter: blur(1.5px);
  mask-image: linear-gradient(
    180deg,
    rgba(0,0,0,0.6),
    transparent 90%
  );
  animation: reflectWave 4s ease-in-out infinite;
}

@@keyframes reflectWave {
  0%, 100% {
    transform: scaleY(-1) scaleX(1);
    filter: blur(1.5px);
  }
  50% {
    transform: scaleY(-1) scaleX(0.99);
    filter: blur(2.5px);
  }
}</code></pre>
            </div>
        </div>

    </div>
</div>
