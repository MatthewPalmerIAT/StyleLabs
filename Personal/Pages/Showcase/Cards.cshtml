@page
@model Personal.Pages.Showcase.CardsModel
@{
    ViewData["Title"] = "Cards";
}

<div class="showcase-page">
    <div class="showcase-header">
        <h1>Card Effects</h1>
        <p>Beautiful card components with glass, 3D tilt, spotlight tracking, animated borders, and immersive water effects.</p>
    </div>

    <div class="showcase-grid">

        <!-- Glass Card -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Glassmorphism <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <div class="card-glass">
                    <div class="card-glass-icon"><i class="fas fa-gem"></i></div>
                    <h3>Glass Card</h3>
                    <p>A frosted glass effect with a rotating conic-gradient border that creates a mesmerizing glow.</p>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.card-glass {
  width: 300px;
  padding: 2rem;
  background: rgba(255,255,255,0.04);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 20px;
  position: relative;
  overflow: hidden;
  transition: all 0.4s ease;
}

/* Rotating gradient border */
.card-glass::before {
  content: '';
  position: absolute;
  top: -50%; left: -50%;
  width: 200%; height: 200%;
  background: conic-gradient(
    from 0deg,
    transparent 0%,
    rgba(0,229,255,0.1) 25%,
    transparent 50%,
    rgba(139,92,246,0.1) 75%,
    transparent 100%
  );
  animation: rotate 8s linear infinite;
  z-index: -1;
}

/* Inner background fill */
.card-glass::after {
  content: '';
  position: absolute;
  inset: 1px;
  background: rgba(8,8,13,0.9);
  border-radius: 19px;
  z-index: -1;
}

@@keyframes rotate {
  to { transform: rotate(360deg); }
}</code></pre>
            </div>
        </div>

        <!-- 3D Tilt Card -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">3D Tilt <span class="badge">CSS + JS</span></h3>
            <div class="preview-panel">
                <div class="card-tilt">
                    <div class="card-tilt-content">
                        <div class="card-tilt-icon"><i class="fas fa-cube"></i></div>
                        <div>Move your cursor around</div>
                    </div>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS + JS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">/* CSS */
.card-tilt {
  width: 300px;
  height: 200px;
  background: linear-gradient(
    135deg,
    rgba(139,92,246,0.1),
    rgba(0,229,255,0.05)
  );
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 20px;
  display: flex;
  align-items: center;
  justify-content: center;
  transform-style: preserve-3d;
  perspective: 1000px;
  cursor: pointer;
  will-change: transform;
}

/* JavaScript */
card.addEventListener('mousemove', (e) => {
  const rect = card.getBoundingClientRect();
  const x = e.clientX - rect.left;
  const y = e.clientY - rect.top;
  const cx = rect.width / 2;
  const cy = rect.height / 2;
  const rx = ((y - cy) / cy) * -12;
  const ry = ((x - cx) / cx) * 12;
  card.style.transform =
    `perspective(1000px) rotateX(${rx}deg) rotateY(${ry}deg)`;
});

card.addEventListener('mouseleave', () => {
  card.style.transform =
    'perspective(1000px) rotateX(0) rotateY(0)';
});</code></pre>
            </div>
        </div>

        <!-- Spotlight Card -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Spotlight Tracking <span class="badge">CSS + JS</span></h3>
            <div class="preview-panel">
                <div class="card-spotlight">
                    <h3><i class="fas fa-sun"></i> Spotlight</h3>
                    <p>Move your cursor over this card to see a radial light follow your mouse position.</p>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS + JS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">/* CSS */
.card-spotlight {
  position: relative;
  width: 300px;
  padding: 2rem;
  background: rgba(255,255,255,0.02);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 20px;
  overflow: hidden;
}

.card-spotlight::before {
  content: '';
  position: absolute;
  width: 250px; height: 250px;
  background: radial-gradient(
    circle,
    rgba(0,229,255,0.15) 0%,
    transparent 70%
  );
  border-radius: 50%;
  pointer-events: none;
  opacity: 0;
  transition: opacity 0.4s ease;
  transform: translate(-50%, -50%);
  left: var(--spot-x, 50%);
  top: var(--spot-y, 50%);
}

.card-spotlight:hover::before { opacity: 1; }

/* JavaScript */
card.addEventListener('mousemove', (e) => {
  const rect = card.getBoundingClientRect();
  card.style.setProperty('--spot-x',
    (e.clientX - rect.left) + 'px');
  card.style.setProperty('--spot-y',
    (e.clientY - rect.top) + 'px');
});</code></pre>
            </div>
        </div>

        <!-- Animated Border Card -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Animated Border <span class="badge">CSS</span></h3>
            <div class="preview-panel">
                <div class="card-animated-border">
                    <h3><i class="fas fa-palette"></i> Rainbow Border</h3>
                    <p>A continuously rotating conic gradient creates a stunning animated border effect around the card.</p>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">@@property --border-angle {
  syntax: '&lt;angle&gt;';
  initial-value: 0deg;
  inherits: false;
}

.card-animated-border {
  position: relative;
  width: 300px;
  padding: 2rem;
  border-radius: 20px;
  background: #0e0e16;
  z-index: 1;
}

/* Gradient border layer */
.card-animated-border::before {
  content: '';
  position: absolute;
  inset: -2px;
  border-radius: 22px;
  background: conic-gradient(
    from var(--border-angle, 0deg),
    #00e5ff, #8b5cf6, #ec4899, #00e5ff
  );
  z-index: -2;
  animation: spin 4s linear infinite;
}

/* Solid fill on top */
.card-animated-border::after {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: 20px;
  background: #0e0e16;
  z-index: -1;
}

@@keyframes spin {
  to { --border-angle: 360deg; }
}</code></pre>
            </div>
        </div>

        <!-- Deep Sea Caustics Card -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Deep Sea Caustics <span class="badge badge-water">CSS</span></h3>
            <div class="preview-panel">
                <div class="card-deep-sea">
                    <div class="card-deep-sea-caustics"></div>
                    <div class="bubble"></div>
                    <div class="bubble"></div>
                    <div class="bubble"></div>
                    <div class="bubble"></div>
                    <div class="bubble"></div>
                    <div class="deep-sea-icon"><i class="fas fa-water"></i></div>
                    <h3>Deep Sea</h3>
                    <p>Animated underwater caustic light patterns with gently rising bubbles create an immersive oceanic depth effect.</p>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.card-deep-sea {
  position: relative;
  width: 300px;
  padding: 2rem;
  background: linear-gradient(180deg, #002233, #001018);
  border: 1px solid rgba(0,229,255,0.1);
  border-radius: 20px;
  overflow: hidden;
}

/* Caustic light layer using a helper div */
.card-deep-sea-caustics {
  position: absolute;
  inset: 0;
  pointer-events: none;
}

.card-deep-sea-caustics::before,
.card-deep-sea-caustics::after {
  content: '';
  position: absolute;
  width: 150%; height: 150%;
  top: -25%; left: -25%;
}

.card-deep-sea-caustics::before {
  background:
    radial-gradient(circle 60px at 30% 40%,
      rgba(0,229,255,0.2), transparent),
    radial-gradient(circle 80px at 75% 25%,
      rgba(0,180,220,0.15), transparent);
  animation: seaCaustic1 8s ease-in-out infinite;
}

@@keyframes seaCaustic1 {
  0%, 100% {
    transform: translate(0, 0) rotate(0deg);
  }
  50% {
    transform: translate(-8px, 12px) rotate(-1deg);
  }
}

/* Bubbles */
.card-deep-sea .bubble {
  position: absolute;
  border-radius: 50%;
  background: radial-gradient(circle at 30% 30%,
    rgba(255,255,255,0.15), transparent);
  border: 1px solid rgba(255,255,255,0.06);
  animation: deepSeaBubble linear infinite;
}

@@keyframes deepSeaBubble {
  0% { bottom: -5%; opacity: 0; }
  10% { opacity: 0.7; }
  100% { bottom: 105%; opacity: 0; }
}</code></pre>
            </div>
        </div>

        <!-- Liquid Glass Card -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Liquid Glass <span class="badge badge-water">CSS</span></h3>
            <div class="preview-panel">
                <div class="card-liquid-glass">
                    <h3><i class="fas fa-tint"></i> Liquid Glass</h3>
                    <p>Dual animated waves flow across the top edge, creating the illusion of a card partially submerged in water.</p>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.card-liquid-glass {
  position: relative;
  width: 300px;
  padding: 2.5rem 2rem 2rem;
  background: linear-gradient(
    180deg,
    rgba(0,60,90,0.4) 0%,
    rgba(8,8,13,0.95) 50%
  );
  border: 1px solid rgba(0,180,220,0.1);
  border-radius: 20px;
  overflow: hidden;
}

/* Wave layer 1 */
.card-liquid-glass::before {
  content: '';
  position: absolute;
  top: -20px; left: -20%;
  width: 140%; height: 60px;
  background: rgba(0,229,255,0.08);
  border-radius: 0 0 50% 50%;
  animation: wave1 5s ease-in-out infinite;
}

/* Wave layer 2 */
.card-liquid-glass::after {
  content: '';
  position: absolute;
  top: -25px; left: -30%;
  width: 160%; height: 55px;
  background: rgba(0,180,220,0.05);
  border-radius: 0 0 45% 55%;
  animation: wave2 4s ease-in-out infinite;
}

@@keyframes wave1 {
  0%, 100% { transform: translateX(0); }
  50% { transform: translateX(8%); }
}

@@keyframes wave2 {
  0%, 100% { transform: translateX(0); }
  50% { transform: translateX(-6%); }
}</code></pre>
            </div>
        </div>

        <!-- Arctic Frost Card -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Arctic Frost <span class="badge" style="background:linear-gradient(135deg,rgba(180,220,255,0.12),rgba(100,180,255,0.15));color:#8ecfff;">CSS</span></h3>
            <div class="preview-panel">
                <div class="card-arctic">
                    <div class="card-arctic-aurora"></div>
                    <div class="card-arctic-frost-layer"></div>
                    <div class="ice-shard"></div>
                    <div class="ice-shard"></div>
                    <div class="ice-shard"></div>
                    <div class="ice-shard"></div>
                    <div class="ice-shard"></div>
                    <div class="arctic-icon"><i class="fas fa-snowflake"></i></div>
                    <h3>Arctic Frost</h3>
                    <p>A frozen landscape with crystalline ice shards that slowly rotate, an aurora borealis ripple, and a spreading frost layer at the edges.</p>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.card-arctic {
  position: relative;
  width: 300px; padding: 2rem;
  background: linear-gradient(165deg, #070e1a, #0b1a30, #0a1225);
  border: 1px solid rgba(140,207,255,0.08);
  border-radius: 20px;
  overflow: hidden;
}

/* Aurora borealis ribbon */
.card-arctic-aurora::before {
  content: '';
  position: absolute;
  top: -60%; left: -20%;
  width: 140%; height: 120%;
  background: linear-gradient(
    180deg, transparent 30%,
    rgba(100,220,255,0.04) 40%,
    rgba(80,200,180,0.06) 45%,
    rgba(120,180,255,0.04) 50%,
    transparent 60%);
  animation: auroraWave 8s ease-in-out infinite;
  filter: blur(20px);
}

@@keyframes auroraWave {
  0%, 100% { transform: translateY(0) skewY(-2deg); }
  50%      { transform: translateY(15px) skewY(2deg); }
}

/* Frost creep from edges */
.card-arctic-frost-layer::before,
.card-arctic-frost-layer::after {
  content: '';
  position: absolute;
  background: radial-gradient(ellipse,
    rgba(180,220,255,0.06), transparent 70%);
  border-radius: 50%;
  animation: frostPulse 6s ease-in-out infinite;
}

/* Ice shards — thin rotated rectangles */
.card-arctic .ice-shard {
  position: absolute;
  background: linear-gradient(180deg,
    rgba(180,220,255,0.15),
    rgba(200,240,255,0.03));
  border-radius: 2px;
  pointer-events: none;
  animation: shardFloat ease-in-out infinite;
}

@@keyframes shardFloat {
  0%, 100% { opacity: 0.3; transform: rotate(var(--r))
    translateY(0); }
  50%      { opacity: 0.7; transform: rotate(var(--r))
    translateY(-8px); }
}</code></pre>
            </div>
        </div>

        <!-- Nebula Storm Card -->
        <div class="showcase-item reveal">
            <h3 class="showcase-item-title">Nebula Storm <span class="badge" style="background:linear-gradient(135deg,rgba(160,80,255,0.12),rgba(100,50,200,0.15));color:#b07fff;">CSS</span></h3>
            <div class="preview-panel">
                <div class="card-nebula">
                    <div class="card-nebula-clouds"></div>
                    <div class="card-nebula-flare"></div>
                    <div class="star-particle"></div>
                    <div class="star-particle"></div>
                    <div class="star-particle"></div>
                    <div class="star-particle"></div>
                    <div class="star-particle"></div>
                    <div class="star-particle"></div>
                    <div class="star-particle"></div>
                    <div class="star-particle"></div>
                    <div class="nebula-icon"><i class="fas fa-meteor"></i></div>
                    <h3>Nebula Storm</h3>
                    <p>Layered cosmic gas clouds rotate and morph while a pulsing core flare radiates outward and stars drift through the void.</p>
                </div>
            </div>
            <div class="code-panel">
                <div class="code-header">
                    <span class="code-lang">CSS</span>
                    <button class="copy-btn" onclick="copyCode(this)">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 17.25v3.375c0 .621-.504 1.125-1.125 1.125h-9.75a1.125 1.125 0 0 1-1.125-1.125V7.875c0-.621.504-1.125 1.125-1.125H6.75a9.06 9.06 0 0 1 1.5.124m7.5 10.376h3.375c.621 0 1.125-.504 1.125-1.125V11.25c0-4.46-3.243-8.161-7.5-8.876a9.06 9.06 0 0 0-1.5-.124H9.375c-.621 0-1.125.504-1.125 1.125v3.5" /></svg>
                        <span class="copy-text">Copy</span>
                    </button>
                </div>
                <pre><code class="language-css">.card-nebula {
  position: relative;
  width: 300px; padding: 2rem;
  background: radial-gradient(ellipse at 30% 50%,
    #1a0836, #0d0520 60%, #08021a);
  border: 1px solid rgba(160,80,255,0.08);
  border-radius: 20px;
  overflow: hidden;
}

/* Dual rotating gas clouds */
.card-nebula-clouds::before {
  content: '';
  position: absolute;
  width: 200%; height: 200%;
  top: -50%; left: -50%;
  background:
    radial-gradient(circle 90px at 40% 45%,
      rgba(139,92,246,0.12), transparent),
    radial-gradient(circle 60px at 65% 60%,
      rgba(236,72,153,0.1), transparent);
  animation: nebulaSwirl1 14s linear infinite;
}

@@keyframes nebulaSwirl1 {
  to { transform: rotate(360deg); }
}

/* Core flare pulse */
.card-nebula-flare::before {
  content: '';
  position: absolute;
  width: 120px; height: 120px;
  top: 50%; left: 50%;
  transform: translate(-50%, -50%);
  background: radial-gradient(circle,
    rgba(160,80,255,0.15), transparent 70%);
  animation: nebulaPulse 4s ease-in-out infinite;
}

@@keyframes nebulaPulse {
  0%, 100% { transform: translate(-50%,-50%) scale(0.8);
    opacity: 0.5; }
  50%      { transform: translate(-50%,-50%) scale(1.3);
    opacity: 1; }
}

/* Star particles with drift */
.card-nebula .star-particle {
  position: absolute;
  border-radius: 50%;
  background: #fff;
  box-shadow: 0 0 3px rgba(255,255,255,0.4);
  animation: starDrift ease-in-out infinite;
}

@@keyframes starDrift {
  0%, 100% { opacity: 0; transform: translate(0,0); }
  30%      { opacity: 1; }
  70%      { opacity: 1; }
  100%     { opacity: 0; transform:
    translate(var(--dx), var(--dy)); }
}</code></pre>
            </div>
        </div>

    </div>
</div>
